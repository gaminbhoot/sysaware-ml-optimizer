[pytest]
addopts = -v --strict-markers --strict-config --maxfail=1
python_files = test_*.py
xfail_strict = true
norecursedirs = live-bench
filterwarnings =
    error
    ignore::DeprecationWarning
    ignore::UserWarning

